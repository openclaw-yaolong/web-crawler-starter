[project]
name = "web-crawler-starter"
version = "0.1.0"
description = "Minimal starter for web crawling"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31.0",
  "beautifulsoup4>=4.12.0",
  "lxml>=5.0.0"
]

[tool.setuptools]
package-dir = {"" = "src"}
